# Model placement

Consult `../../../MODEL_INDEX.tsv` for the exact model family and its status.
Place externally supplied model files in this directory without changing the
filenames expected by the corresponding experiment scripts.
